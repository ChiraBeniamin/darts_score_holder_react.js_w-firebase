import pages from './pages';
import archived from './archived';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [pages, archived]
};

export default menuItems;
